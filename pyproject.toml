[project]
name = "agent-demo-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "dashscope>=1.27.5",
    "faiss-cpu>=1.12.0",
    "fastapi>=0.141.1",
    "mineru[all]>=3.4.5",
    "openai>=2.54.0",
]

[project.scripts]
agent-demo-1 = "agent_demo_1:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
